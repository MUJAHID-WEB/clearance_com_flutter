import 'package:flutter/material.dart';

class UpdateApplicationLayout extends StatelessWidget {
  const UpdateApplicationLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
